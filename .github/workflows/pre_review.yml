name: CI

on:
  push:
    branches: [ $default-branch, main, master, develop ]
  pull_request:
    branches: [ $default-branch, main, master, develop ]

permissions:
  contents: read

jobs:
  build:
    name: Format / Lint / Test
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [3.11]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ matrix.python-version }}-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-${{ matrix.python-version }}-

      - name: Create virtualenv and install dependencies
        run: |
          python -m venv .venv
          . .venv/bin/activate
          python -m pip install --upgrade pip setuptools wheel
          if [ -f requirements.txt ]; then
            pip install -r requirements.txt
          elif [ -f pyproject.toml ]; then
            pip install .
          else
            echo "No requirements.txt or pyproject.toml found — skipping dependency install"
          fi

      - name: Install developer tools
        run: |
          . .venv/bin/activate
          python -m pip install --upgrade black pylint pytest

      - name: Format (Black) — autoformat to avoid check failures
        run: |
          . .venv/bin/activate
          black --version
          # Auto-format repository so CI won't fail on formatting differences.
          black .

      - name: Lint (pylint) — report issues but don't fail the job
        continue-on-error: true
        run: |
          . .venv/bin/activate
          pylint --version
          pylint logic

      - name: Run tests (pytest)
        run: |
          . .venv/bin/activate
          pytest -q